import { ItemArticleSelected } from './item-article-selected';

describe('ItemArticleSelected', () => {
  it('should create an instance', () => {
    expect(new ItemArticleSelected()).toBeTruthy();
  });
});
